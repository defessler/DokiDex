using System.Windows.Controls;

namespace DokiDex.Control.Views;

// DokiGen Studio — a thin page over `doki gen` (describe → pick a kind → Generate → preview → remix).
// All state lives on StudioViewModel; the code-behind is just the XAML loader (no view logic in Phase 1).
public partial class StudioView : UserControl
{
    public StudioView() => InitializeComponent();
}
