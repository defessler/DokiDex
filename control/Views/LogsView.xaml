<UserControl x:Class="DokiCode.Control.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <UserControl.Resources>
    <Style x:Key="DarkBox" TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource BgBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="CaretBrush" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="6,3"/>
      <Setter Property="FontFamily" Value="Consolas"/>
    </Style>
    <Style x:Key="Toggle" TargetType="ToggleButton">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="{StaticResource Surface2Brush}"/>
      <Setter Property="Padding" Value="10,4"/>
      <Setter Property="Margin" Value="6,0,0,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ToggleButton">
            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}"
                    BorderThickness="1" BorderBrush="Transparent">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentBrush}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="BorderBrush" Value="#80E8C77A"/>   <!-- gold hover; cyan reserved for the IsChecked 'on' fill -->
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <!-- tab item -->
    <Style x:Key="TabItem" TargetType="ListBoxItem">
      <Setter Property="Foreground" Value="{StaticResource DimBrush}"/>
      <Setter Property="Padding" Value="10,4"/>
      <Setter Property="Margin" Value="0,0,4,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="bd" Background="Transparent" CornerRadius="4" Padding="{TemplateBinding Padding}">
              <Grid>
                <ContentPresenter/>
                <Rectangle x:Name="sel" Height="2" VerticalAlignment="Bottom" Fill="{StaticResource TealBrush}" Opacity="0"/>
              </Grid>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True">   <!-- selected = a 2px cyan underline, not a fill -->
                <Setter TargetName="sel" Property="Opacity" Value="1"/>
                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>

  <DockPanel Background="{StaticResource BgBrush}">

    <!-- toolbar -->
    <Border DockPanel.Dock="Top" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush2}"
            BorderThickness="0,0,0,1" Padding="14,9">
      <DockPanel>
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
          <TextBlock Text="filter:" Foreground="{StaticResource DimBrush}" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="11"/>
          <TextBox Width="160" Style="{StaticResource DarkBox}" VerticalAlignment="Center"
                   Text="{Binding Logs.Filter, UpdateSourceTrigger=PropertyChanged}" ToolTip="regex or substring; matches dim out the rest"/>
          <ToggleButton Content="⏸ pause" Style="{StaticResource Toggle}" IsChecked="{Binding Logs.Paused}"/>
          <ToggleButton Content="stderr" Style="{StaticResource Toggle}" IsChecked="{Binding Logs.IncludeStderr}"/>
        </StackPanel>
        <ListBox ItemsSource="{Binding Logs.Tabs}" SelectedItem="{Binding Logs.SelectedTab}"
                 Background="Transparent" BorderThickness="0" ItemContainerStyle="{StaticResource TabItem}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" VerticalAlignment="Center">
          <ListBox.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ListBox.ItemsPanel>
        </ListBox>
      </DockPanel>
    </Border>

    <!-- log lines -->
    <ListBox x:Name="LogList" ItemsSource="{Binding Logs.View}" Background="{StaticResource BgBrush}"
             BorderThickness="0" Foreground="{StaticResource TextBrush}"
             ScrollViewer.HorizontalScrollBarVisibility="Auto" VirtualizingStackPanel.IsVirtualizing="True">
      <ListBox.ItemContainerStyle>
        <Style TargetType="ListBoxItem">
          <Setter Property="Background" Value="Transparent"/>
          <Setter Property="Padding" Value="0"/>
          <Setter Property="Margin" Value="0"/>
          <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="ListBoxItem">
                <Border Background="Transparent" Padding="14,1"><ContentPresenter/></Border>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </ListBox.ItemContainerStyle>
      <ListBox.ItemTemplate>
        <DataTemplate>
          <DockPanel LastChildFill="True">
            <TextBlock DockPanel.Dock="Left" Text="{Binding Time}" Foreground="{StaticResource DimBrush}"
                       FontFamily="Consolas" FontSize="11" Margin="0,0,8,0"/>
            <TextBlock DockPanel.Dock="Left" Text="{Binding Service}" Foreground="{StaticResource DimBrush}"
                       FontFamily="Consolas" FontSize="11" Width="116" TextTrimming="CharacterEllipsis" Margin="0,0,8,0"/>
            <TextBlock Text="{Binding Text}" Foreground="{Binding Severity, Converter={StaticResource SeverityBrush}}"
                       FontFamily="Consolas" FontSize="11.5"/>
          </DockPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </DockPanel>
</UserControl>
