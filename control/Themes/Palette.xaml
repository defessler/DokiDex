<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:DokiCode.Control.Converters">

  <!-- ============ palette (VS-Code dark, inherited from control.ps1) ============ -->
  <Color x:Key="BgColor">#FF1E1E1E</Color>
  <SolidColorBrush x:Key="BgBrush"      Color="#FF1E1E1E"/>
  <SolidColorBrush x:Key="SurfaceBrush" Color="#FF252526"/>
  <SolidColorBrush x:Key="Surface2Brush" Color="#FF2D2D30"/>
  <SolidColorBrush x:Key="Surface3Brush" Color="#FF333337"/>
  <SolidColorBrush x:Key="BorderBrush2" Color="#FF3F3F46"/>
  <SolidColorBrush x:Key="TextBrush"    Color="#FFD4D4D4"/>
  <SolidColorBrush x:Key="DimBrush"     Color="#FF858585"/>
  <SolidColorBrush x:Key="AccentBrush"  Color="#FF0E639C"/>
  <SolidColorBrush x:Key="TealBrush"    Color="#FF4EC9B0"/>
  <SolidColorBrush x:Key="AmberBrush"   Color="#FFD7BA7D"/>
  <SolidColorBrush x:Key="DangerBrush"  Color="#FFA1260D"/>
  <SolidColorBrush x:Key="DangerBrightBrush" Color="#FFF14C4C"/>
  <SolidColorBrush x:Key="InfoBrush"    Color="#FF9CDCFE"/>

  <!-- ============ converters ============ -->
  <conv:StateKindToBrushConverter x:Key="StateBrush"/>
  <conv:SeverityToBrushConverter x:Key="SeverityBrush"/>
  <conv:GroupToAccentConverter x:Key="GroupAccent"/>
  <conv:ModeActiveBrushConverter x:Key="ModeActiveBrush"/>
  <conv:BoolToOpacityConverter x:Key="BoolOpacity"/>
  <conv:BoolToVisibilityConverter x:Key="BoolVis"/>
  <conv:InverseBoolToVisibilityConverter x:Key="InvBoolVis"/>
  <conv:WarnFlagToBrushConverter x:Key="WarnBrush"/>

  <!-- ============ text ============ -->
  <Style x:Key="H1" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    <Setter Property="FontSize" Value="16"/>
    <Setter Property="FontWeight" Value="Bold"/>
  </Style>
  <Style x:Key="SectionHeader" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource DimBrush}"/>
    <Setter Property="FontSize" Value="11"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Margin" Value="2,0,0,6"/>
  </Style>
  <Style x:Key="Mono" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="Cascadia Mono, Consolas"/>
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
  </Style>

  <!-- ============ buttons (ported from control.ps1) ============ -->
  <Style x:Key="Btn" TargetType="Button">
    <Setter Property="Foreground" Value="#FFFFFFFF"/>
    <Setter Property="Background" Value="#FF3A3D41"/>
    <Setter Property="Padding" Value="12,6"/>
    <Setter Property="Margin" Value="0,0,6,0"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="ToolTipService.InitialShowDelay" Value="300"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4"
                  BorderThickness="1" BorderBrush="Transparent" Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="BorderBrush" Value="#B0FFFFFF"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="bd" Property="Opacity" Value="0.75"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="bd" Property="Opacity" Value="0.4"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="BtnPrimary" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
  </Style>
  <Style x:Key="BtnDanger" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
  </Style>
  <Style x:Key="BtnGhost" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="#FF2D2D30"/>
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
  </Style>

  <!-- small square icon button for card actions -->
  <Style x:Key="IconBtn" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="#FF2D2D30"/>
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    <Setter Property="Padding" Value="8,4"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Margin" Value="0,0,5,0"/>
  </Style>

  <!-- segmented mode button: accent border when its mode is active -->
  <Style x:Key="ModeBtn" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="#FF2D2D30"/>
    <Setter Property="Padding" Value="14,8"/>
    <Setter Property="MinWidth" Value="110"/>
  </Style>

  <!-- nav rail button (left rail) -->
  <Style x:Key="NavBtn" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
    <Setter Property="Padding" Value="10,7"/>
    <Setter Property="Margin" Value="0,0,0,2"/>
    <Setter Property="FontSize" Value="13"/>
  </Style>

  <!-- card shell -->
  <Style x:Key="CardBorder" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource Surface2Brush}"/>
    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="6"/>
    <Setter Property="Padding" Value="12"/>
    <Setter Property="Margin" Value="0,0,10,10"/>
  </Style>

</ResourceDictionary>
