<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:DokiDex.Control.Converters">

  <!-- ============ DokiDex premium theme — the splash aesthetic, app-wide ============
       Deep VOID field; ONE emitting accent (reactor cyan = live/active/primary); GOLD as etched
       structure that never glows (section labels, the MEDIA group, dividers). Functional status
       colors (amber/red) are deliberately KEPT — telling you a service is DOWN is the panel's job
       (the boot stays monochrome; the dashboard is where red/amber legitimately live). -->
  <Color x:Key="BgColor">#FF0A0E14</Color>
  <SolidColorBrush x:Key="BgBrush"      Color="#FF0A0E14"/>   <!-- app field (lifted from boot void for readability) -->
  <SolidColorBrush x:Key="SurfaceBrush" Color="#FF0D121B"/>   <!-- rails, strips, mode panel -->
  <SolidColorBrush x:Key="Surface2Brush" Color="#FF121826"/>  <!-- cards (sit above the field) -->
  <SolidColorBrush x:Key="Surface3Brush" Color="#FF1B2433"/>  <!-- inputs / hover lift -->
  <SolidColorBrush x:Key="BorderBrush2" Color="#FF243140"/>   <!-- hairline, cool -->
  <SolidColorBrush x:Key="TextBrush"    Color="#FFE6EEF6"/>   <!-- cool white -->
  <SolidColorBrush x:Key="DimBrush"     Color="#FF7E8C99"/>   <!-- cool grey (matches boot) -->
  <SolidColorBrush x:Key="AccentBrush"  Color="#FF0E7490"/>   <!-- deep cyan (the rare emphatic fill) -->
  <SolidColorBrush x:Key="TealBrush"    Color="#FF35E0F0"/>   <!-- LLM group / live = the emitting cyan -->
  <SolidColorBrush x:Key="AmberBrush"   Color="#FFE8C77A"/>   <!-- MEDIA group / structure = etched gold -->
  <SolidColorBrush x:Key="DangerBrush"  Color="#FF7A2230"/>   <!-- down (deep) -->
  <SolidColorBrush x:Key="DangerBrightBrush" Color="#FFFF6B6B"/> <!-- crashed (the one alarm signal) -->

  <!-- ============ boot sequence: arc-reactor / LCARS / FF accents ============ -->
  <!-- deep-space field the reactor ignites against (Anthropic/OpenAI restraint = lots of void) -->
  <Color x:Key="VoidColor">#FF05070B</Color>
  <SolidColorBrush x:Key="VoidBrush"     Color="#FF05070B"/>
  <SolidColorBrush x:Key="VoidEdgeBrush" Color="#FF0B0F16"/>
  <!-- the one igniting accent (Iron Man reactor cyan) + its companions -->
  <Color x:Key="ReactorColor">#FF35E0F0</Color>
  <SolidColorBrush x:Key="ReactorBrush"    Color="#FF35E0F0"/>
  <SolidColorBrush x:Key="ReactorDimBrush" Color="#7035E0F0"/>
  <Color x:Key="ReactorGoldColor">#FFE8C77A</Color>
  <SolidColorBrush x:Key="ReactorGoldBrush" Color="#FFE8C77A"/>
  <SolidColorBrush x:Key="HudFaintBrush"   Color="#2A6FE9F5"/>
  <SolidColorBrush x:Key="BootTextBrush"   Color="#FFEAF6FF"/>
  <!-- the boot SigilStar hexagram, shared as the app brand mark (caption + empty-state) -->
  <Geometry x:Key="SigilGeo">M150,18 L264,84 L264,216 L150,282 L36,216 L36,84 Z M150,40 L242,200 L58,200 Z M150,260 L58,100 L242,100 Z</Geometry>

  <!-- ============ converters ============ -->
  <conv:StateKindToBrushConverter x:Key="StateBrush"/>
  <conv:SeverityToBrushConverter x:Key="SeverityBrush"/>
  <conv:GroupToAccentConverter x:Key="GroupAccent"/>
  <conv:ModeActiveBrushConverter x:Key="ModeActiveBrush"/>
  <conv:BoolToOpacityConverter x:Key="BoolOpacity"/>
  <conv:BoolToVisibilityConverter x:Key="BoolVis"/>
  <conv:InverseBoolToVisibilityConverter x:Key="InvBoolVis"/>
  <conv:WarnFlagToBrushConverter x:Key="WarnBrush"/>
  <conv:ModeMatchConverter x:Key="ModeMatch"/>
  <conv:ZeroToVisConverter x:Key="ZeroToVis"/>

  <!-- ============ text ============ -->
  <Style x:Key="H1" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    <Setter Property="FontSize" Value="16"/>
    <Setter Property="FontWeight" Value="Bold"/>
  </Style>
  <Style x:Key="SectionHeader" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource DimBrush}"/>
    <Setter Property="FontSize" Value="11"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Margin" Value="2,0,0,6"/>
  </Style>
  <Style x:Key="Mono" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="Cascadia Mono, Consolas"/>
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
  </Style>

  <!-- ============ buttons (ported from control.ps1) ============ -->
  <Style x:Key="Btn" TargetType="Button">
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    <Setter Property="Background" Value="{StaticResource Surface3Brush}"/>
    <Setter Property="Padding" Value="12,6"/>
    <Setter Property="Margin" Value="0,0,6,0"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="ToolTipService.InitialShowDelay" Value="300"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4"
                  BorderThickness="1" BorderBrush="Transparent" Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="BorderBrush" Value="#80E8C77A"/>   <!-- gold = structure hover; cyan is reserved for active/live -->
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="bd" Property="Opacity" Value="0.75"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="bd" Property="Opacity" Value="0.4"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="BtnPrimary" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
  </Style>
  <Style x:Key="BtnDanger" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
  </Style>
  <Style x:Key="BtnGhost" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="{StaticResource Surface2Brush}"/>
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
  </Style>

  <!-- small square icon button for card actions -->
  <Style x:Key="IconBtn" TargetType="Button" BasedOn="{StaticResource Btn}">
    <Setter Property="Background" Value="{StaticResource Surface2Brush}"/>
    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
    <Setter Property="Padding" Value="8,4"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Margin" Value="0,0,5,0"/>
  </Style>

  <!-- segmented mode button: active = a 2px CYAN left edge + Surface3 lift (NOT a muddy accent fill);
       hover = a GOLD structure border. Active is driven per-button in DashboardView via ModeMatch
       (ActiveMode == CommandParameter) on the EdgeOn Tag, so the real Tag stays free for ModeHover. -->
  <Style x:Key="ModeBtn" TargetType="Button">
    <Setter Property="Foreground" Value="{StaticResource DimBrush}"/>
    <Setter Property="Padding" Value="14,8"/>
    <Setter Property="MinWidth" Value="110"/>
    <Setter Property="Margin" Value="0,0,6,0"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="bd" Background="{StaticResource Surface2Brush}" CornerRadius="5"
                  BorderThickness="1" BorderBrush="{StaticResource BorderBrush2}">
            <Grid>
              <Rectangle x:Name="edge" Width="2" HorizontalAlignment="Left" Fill="{StaticResource TealBrush}" RadiusX="1" RadiusY="1" Opacity="0"/>
              <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="BorderBrush" Value="#80E8C77A"/>
            </Trigger>
            <!-- ACTIVE = ActiveMode == this button's CommandParameter (via ModeMatch). Drives off the
                 VM, NOT Tag (which ModeHover owns), and NOT the frozen-brush equality of ModeActiveBrush. -->
            <DataTrigger Value="True">
              <DataTrigger.Binding>
                <MultiBinding Converter="{StaticResource ModeMatch}">
                  <Binding Path="DataContext.ActiveMode" RelativeSource="{RelativeSource TemplatedParent}"/>
                  <Binding Path="CommandParameter" RelativeSource="{RelativeSource TemplatedParent}"/>
                </MultiBinding>
              </DataTrigger.Binding>
              <Setter TargetName="edge" Property="Opacity" Value="1"/>
              <Setter TargetName="bd" Property="Background" Value="{StaticResource Surface3Brush}"/>
              <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            </DataTrigger>
            <Trigger Property="IsPressed" Value="True"><Setter TargetName="bd" Property="Opacity" Value="0.8"/></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- nav rail button: active VIEW = a 2px cyan left edge (Tag='active', toggled in code-behind);
       hover = quiet text + a barely-there wash (no cyan). -->
  <Style x:Key="NavBtn" TargetType="Button">
    <Setter Property="Foreground" Value="{StaticResource DimBrush}"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
    <Setter Property="Padding" Value="12,7"/>
    <Setter Property="Margin" Value="0,0,0,2"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="bd" Background="Transparent" CornerRadius="4">
            <Grid>
              <Rectangle x:Name="edge" Width="2" HorizontalAlignment="Left" Fill="{StaticResource TealBrush}" Opacity="0"/>
              <ContentPresenter Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <!-- wash only on hover (no foreground change) so the colored GLOBAL actions keep their
                 signal color; the strong cyan active-edge below is the orientation signal. -->
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Background" Value="#14E6EEF6"/>
            </Trigger>
            <Trigger Property="Tag" Value="active">
              <Setter TargetName="edge" Property="Opacity" Value="1"/>
              <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- card shell -->
  <Style x:Key="CardBorder" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource Surface2Brush}"/>
    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="6"/>
    <Setter Property="Padding" Value="12"/>
    <Setter Property="Margin" Value="0,0,10,10"/>
  </Style>

</ResourceDictionary>
